#!/bin/sh
# X 会话启动脚本。目标：让访客在画面出现的第一秒就看到"这是个完整系统"。
#
# 顺序有讲究 —— 先铺壁纸再起 WM，避免中间闪一下黑屏。

# 渐变壁纸。用 xsetroot 画纯色而不是放图片文件，省几十 MB 且加载更快。
# 深蓝灰色调，和站点主题一致。
xsetroot -solid "#1a2332"

# 光标改成标准箭头 —— 默认是 X 那个大黑十字，看着像没加载完
xsetroot -cursor_name left_ptr

# 起窗口管理器（前台运行，它退出则会话结束）
exec jwm
